require "spec_helper"

describe MyCrawler do
  it "has a version number" do
    expect(MyCrawler::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
